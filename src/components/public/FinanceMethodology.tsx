import React from 'react';
import { ArrowLeft, CheckCircle2, Info } from 'lucide-react';

export function FinanceMethodology({ onNavigate }: { onNavigate: (path: string) => void }) {
  const terms = [['Allocation', 'A funding amount recorded against a project and source.'], ['Commitment', 'A contractual amount recorded against a project, distinct from cash paid.'], ['Tranche', 'An approved portion of a commitment scheduled for disbursement.'], ['Disbursement', 'A payment record marked as disbursed. It is not automatically expenditure.'], ['Reported expenditure', 'An amount submitted against a disbursement. It remains unverified until an authorized review marks it verified.'], ['Remaining balance', 'The recorded allocation or commitment less valid disbursements in the same currency.']];
  return <div className="max-w-5xl mx-auto w-full px-4 sm:px-6 lg:px-8 py-10 space-y-8"><button onClick={() => onNavigate('/analytics')} className="inline-flex items-center gap-2 text-sm font-bold text-emerald-800"><ArrowLeft className="h-4 w-4" /> Public analytics</button><header><p className="text-xs font-bold uppercase tracking-[0.18em] text-emerald-700">Phase 11 methodology</p><h1 className="text-3xl sm:text-4xl font-black text-slate-900 mt-2">Understanding project finances</h1><p className="text-slate-600 mt-3 max-w-3xl">GhanaBuild separates each fiscal step so citizens can see what is allocated, committed, approved, disbursed, and verified without treating one as another.</p></header><div className="grid sm:grid-cols-2 gap-4">{terms.map(([term, description]) => <div key={term} className="bg-white border border-slate-200 rounded-2xl p-5"><h2 className="font-black text-lg">{term}</h2><p className="text-sm text-slate-600 mt-2">{description}</p></div>)}</div><section className="bg-amber-50 border border-amber-200 rounded-2xl p-6"><div className="flex gap-3"><Info className="h-5 w-5 text-amber-800 shrink-0" /><div><h2 className="font-black text-amber-950">Attention indicators are not accusations</h2><p className="text-sm text-amber-950 mt-2">A financial and physical progress variance is an informational review prompt. It does not establish fraud, corruption, misappropriation, or contractor fault. Formal extensions and accounting-system reconciliation are not represented unless the data model contains them.</p></div></div></section><section className="bg-white border border-slate-200 rounded-2xl p-6 space-y-3"><h2 className="text-xl font-black">Data limitations</h2>{['Only records approved for public visibility appear on public project pages.', 'Currencies are never silently converted. Aggregation is limited to the project currency.', 'Reported expenditure is clearly separated from verified expenditure.', 'Corrections preserve history through reversal or rejection rather than hard deletion.', 'GhanaBuild is a transparency and monitoring platform, not automatically an official government accounting authority.'].map((item) => <p key={item} className="flex gap-3 text-sm text-slate-700"><CheckCircle2 className="h-5 w-5 text-emerald-700 shrink-0" />{item}</p>)}</section></div>;
}
