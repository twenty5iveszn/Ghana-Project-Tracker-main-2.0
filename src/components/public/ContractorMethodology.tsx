import React from 'react';
import { ArrowLeft, CheckCircle2, Info } from 'lucide-react';

interface ContractorMethodologyProps { onNavigate: (path: string) => void; }
export function ContractorMethodology({ onNavigate }: ContractorMethodologyProps) {
  return <div className="max-w-5xl mx-auto w-full px-4 sm:px-6 lg:px-8 py-10 space-y-8"><button onClick={() => onNavigate('/contractors')} className="inline-flex items-center gap-2 text-sm font-bold text-emerald-800"><ArrowLeft className="h-4 w-4" /> Contractor directory</button><header><p className="text-xs font-bold uppercase tracking-[0.18em] text-emerald-700">Transparent methodology</p><h1 className="text-3xl sm:text-4xl font-black text-slate-900 mt-2">GhanaBuild Performance Indicator</h1><p className="text-slate-600 mt-3 max-w-3xl">A neutral analytical summary of recorded public project data. It is not an official government rating, procurement finding, or conclusion about contractor fault.</p></header><section className="grid sm:grid-cols-2 gap-4">{[['Delivery', '40%', 'Completion rate across associated projects.'], ['Progress', '25%', 'Average recorded progress across active projects.'], ['Community response', '20%', 'Resolution rate for community reports, when reports exist.'], ['Reliability', '15%', 'A descriptive signal based on abandoned and on-hold project records.']].map(([title, weight, description]) => <div key={title} className="bg-white border border-slate-200 rounded-2xl p-5"><div className="flex justify-between"><h2 className="font-black">{title}</h2><span className="font-black text-emerald-700">{weight}</span></div><p className="text-sm text-slate-600 mt-2">{description}</p></div>)}</section><section className="bg-white border border-slate-200 rounded-2xl p-6 space-y-4"><h2 className="text-xl font-black">Metric rules</h2>{['On-time delivery uses completed projects with both recorded dates and compares actual completion with expected completion.', 'Delayed means an unfinished, non-cancelled project past its recorded expected completion date. Formal extensions are not represented in the current data model.', 'Community reports remain community observations. High-severity and poor-workmanship reports are not verified findings unless separately verified through the platform workflow.', 'Missing dates, budgets, progress, or reports are excluded from the applicable calculation rather than silently treated as zero.', 'Confidence is shown with the number of projects evaluated: one project is preliminary, two to three are moderate, and four or more are high coverage.'].map((text) => <p key={text} className="flex gap-3 text-sm text-slate-700"><CheckCircle2 className="h-5 w-5 text-emerald-700 shrink-0" />{text}</p>)}</section><div className="flex gap-3 bg-emerald-50 border border-emerald-200 rounded-2xl p-5 text-sm text-emerald-950"><Info className="h-5 w-5 shrink-0" />Indicators update when the underlying project, report, and verification records change. Public profiles include verified projects only.</div></div>;
}
